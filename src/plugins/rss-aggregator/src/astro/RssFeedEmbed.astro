---
import FeedList from "./FeedList.astro";

interface Props {
	node?: {
		id?: string;
		limit?: number;
	};
}

const { node } = Astro.props;
---

<FeedList displayId={node?.id || "default"} limit={node?.limit} />
