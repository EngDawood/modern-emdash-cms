diff --git a/dist/connection-B4zVnQIa.mjs b/dist/connection-B4zVnQIa.mjs
index ade9bbb3d89555ee2c21bcaf23d8764e3792e46a..214aeff8cad1557b5125efe8577833831e5666e7 100644
--- a/dist/connection-B4zVnQIa.mjs
+++ b/dist/connection-B4zVnQIa.mjs
@@ -1,5 +1,5 @@
-import BetterSqlite3 from "better-sqlite3";
-import { Kysely, SqliteDialect } from "kysely";
+import { LibsqlDialect } from "@libsql/kysely-libsql";
+import { Kysely } from "kysely";
 
 //#region src/database/connection.ts
 var EmDashDatabaseError = class extends Error {
@@ -12,21 +12,19 @@ var EmDashDatabaseError = class extends Error {
 /**
 * Creates a Kysely database instance
 * Supports:
-* - file:./path/to/db.sqlite (local SQLite)
+* - file:./path/to/db.sqlite (local SQLite via libsql)
 * - :memory: (in-memory SQLite for testing)
-* - libsql://... (Turso/libSQL with auth token) - TODO
+* - libsql://... (Turso/libSQL with auth token)
 */
 function createDatabase(config) {
 	try {
 		if (config.url.startsWith("file:") || config.url === ":memory:") {
-			const sqlite = new BetterSqlite3(config.url === ":memory:" ? ":memory:" : config.url.replace("file:", ""));
-			sqlite.pragma("journal_mode = WAL");
-			sqlite.pragma("foreign_keys = ON");
-			return new Kysely({ dialect: new SqliteDialect({ database: sqlite }) });
+			const url = config.url === ":memory:" ? ":memory:" : config.url;
+			return new Kysely({ dialect: new LibsqlDialect({ url }) });
 		}
 		if (config.url.startsWith("libsql:")) {
 			if (!config.authToken) throw new EmDashDatabaseError("Auth token required for remote libSQL database");
-			throw new EmDashDatabaseError("LibSQL not yet implemented");
+			return new Kysely({ dialect: new LibsqlDialect({ url: config.url, authToken: config.authToken }) });
 		}
 		throw new EmDashDatabaseError(`Unsupported database URL scheme: ${config.url}`);
 	} catch (error) {
diff --git a/src/astro/routes/api/auth/oauth/[provider]/callback.ts b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
index 7c69cd61340b9ba0fe5d467dfe99fe7117b549d9..e30f0b7ecc4202f72286818adeded3fc469864eb 100644
--- a/src/astro/routes/api/auth/oauth/[provider]/callback.ts
+++ b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
@@ -19,6 +19,9 @@ import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 
 import { createOAuthStateStore } from "#auth/oauth-state-store.js";
 
+// Astro v6: locals.runtime.env was removed. Use cloudflare:workers for env access.
+import { env as _cfEnv } from "cloudflare:workers";
+
 type ProviderName = "github" | "google";
 
 const VALID_PROVIDERS = new Set<string>(["github", "google"]);
@@ -113,11 +116,9 @@ export const GET: APIRoute = async ({ params, request, locals, session, redirect
 
 	try {
 		// Get OAuth providers from environment
-		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion) -- locals.runtime is injected by the Cloudflare adapter at runtime; not declared on App.Locals since the adapter is optional
-		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
-		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion) -- import.meta.env is typed as ImportMetaEnv but we need Record<string, unknown> for getOAuthConfig
-		const env = runtimeLocals.runtime?.env ?? (import.meta.env as Record<string, unknown>);
-		const providers = getOAuthConfig(env);
+		// Astro v6 + Cloudflare adapter: use cloudflare:workers env (static import above)
+		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion)
+		const providers = getOAuthConfig(_cfEnv as unknown as Record<string, unknown>);
 
 		if (!providers[provider]) {
 			return redirect(
diff --git a/src/astro/routes/api/auth/oauth/[provider].ts b/src/astro/routes/api/auth/oauth/[provider].ts
index d8150d5c6d37f00dee3fc2fa17775ae41cbadc4d..fc27a58b7fd39b0bd892dc53dff34e131b4d5ec9 100644
--- a/src/astro/routes/api/auth/oauth/[provider].ts
+++ b/src/astro/routes/api/auth/oauth/[provider].ts
@@ -12,6 +12,9 @@ import { createAuthorizationUrl, type OAuthConsumerConfig } from "@emdash-cms/au
 
 import { createOAuthStateStore } from "#auth/oauth-state-store.js";
 
+// Astro v6: locals.runtime.env was removed. Use cloudflare:workers for env access.
+import { env as _cfEnv } from "cloudflare:workers";
+
 type ProviderName = "github" | "google";
 
 const VALID_PROVIDERS = new Set<string>(["github", "google"]);
@@ -87,12 +90,9 @@ export const GET: APIRoute = async ({ params, request, locals, redirect }) => {
 		const url = new URL(request.url);
 
 		// Get OAuth providers from environment
-		// Access via locals.runtime for Cloudflare, or import.meta.env for Node
-		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion) -- locals.runtime is injected by the Cloudflare adapter at runtime; not declared on App.Locals since the adapter is optional
-		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
-		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion) -- import.meta.env is typed as ImportMetaEnv but we need Record<string, unknown> for getOAuthConfig
-		const env = runtimeLocals.runtime?.env ?? (import.meta.env as Record<string, unknown>);
-		const providers = getOAuthConfig(env);
+		// Astro v6 + Cloudflare adapter: use cloudflare:workers env (static import above)
+		// eslint-disable-next-line typescript-eslint(no-unsafe-type-assertion)
+		const providers = getOAuthConfig(_cfEnv as unknown as Record<string, unknown>);
 
 		if (!providers[provider]) {
 			return redirect(
