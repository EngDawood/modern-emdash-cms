diff --git a/dist/astro/routes/api/auth/oauth/_provider_/callback.mjs b/dist/astro/routes/api/auth/oauth/_provider_/callback.mjs
index 7a77fc690c193abc2ed1ebc91f2e4e40125bef2a..1a5e70b80922c93245890dc0ea5565a7cad6914d 100644
--- a/dist/astro/routes/api/auth/oauth/_provider_/callback.mjs
+++ b/dist/astro/routes/api/auth/oauth/_provider_/callback.mjs
@@ -4,6 +4,7 @@ import { t as finalizeSetup } from "../../../../../../setup-complete-VoEZfasi.mj
 import { t as createOAuthStateStore } from "../../../../../../oauth-state-store-BJ7YtrfD.mjs";
 import { createKyselyAdapter } from "@emdash-cms/auth/adapters/kysely";
 import { OAuthError, Role, handleOAuthCallback } from "@emdash-cms/auth";
+import { env as _cfEnv } from "cloudflare:workers";
 
 //#region src/astro/routes/api/auth/oauth/[provider]/callback.ts
 const prerender = false;
@@ -53,7 +54,7 @@ const GET = async ({ params, request, locals, session, redirect }) => {
 	}
 	if (!code || !state) return redirect(`/_emdash/admin/login?error=invalid_callback&message=${encodeURIComponent("Missing code or state parameter")}`);
 	try {
-		const providers = getOAuthConfig(locals.runtime?.env ?? import.meta.env);
+		const providers = getOAuthConfig(_cfEnv ?? import.meta.env);
 		if (!providers[provider]) return redirect(`/_emdash/admin/login?error=provider_not_configured&message=${encodeURIComponent(`OAuth provider ${provider} is not configured`)}`);
 		const adapter = createKyselyAdapter(emdash.db);
 		const stateStore = createOAuthStateStore(emdash.db);
diff --git a/dist/astro/routes/api/auth/oauth/_provider_.mjs b/dist/astro/routes/api/auth/oauth/_provider_.mjs
index d89831c9ae42aeba09d01a0111cd4987129fb54d..116f6b9fa4f38b3f8a2c1bdb205848e8198ca58d 100644
--- a/dist/astro/routes/api/auth/oauth/_provider_.mjs
+++ b/dist/astro/routes/api/auth/oauth/_provider_.mjs
@@ -1,6 +1,7 @@
 import { n as getPublicOrigin } from "../../../../../public-url-egRHCy1m.mjs";
 import { t as createOAuthStateStore } from "../../../../../oauth-state-store-BJ7YtrfD.mjs";
 import { createAuthorizationUrl } from "@emdash-cms/auth";
+import { env as _cfEnv } from "cloudflare:workers";
 
 //#region src/astro/routes/api/auth/oauth/[provider].ts
 const prerender = false;
@@ -42,7 +43,7 @@ const GET = async ({ params, request, locals, redirect }) => {
 	if (!emdash?.db) return redirect(`${errorRedirectBase}?error=server_error&message=${encodeURIComponent("Database not configured")}`);
 	try {
 		const url = new URL(request.url);
-		const providers = getOAuthConfig(locals.runtime?.env ?? import.meta.env);
+		const providers = getOAuthConfig(_cfEnv ?? import.meta.env);
 		if (!providers[provider]) return redirect(`${errorRedirectBase}?error=provider_not_configured&message=${encodeURIComponent(`OAuth provider ${provider} is not configured. Set either EMDASH_OAUTH_${provider.toUpperCase()}_CLIENT_ID and EMDASH_OAUTH_${provider.toUpperCase()}_CLIENT_SECRET, or ${provider.toUpperCase()}_CLIENT_ID and ${provider.toUpperCase()}_CLIENT_SECRET.`)}`);
 		const { url: authUrl } = await createAuthorizationUrl({
 			baseUrl: `${getPublicOrigin(url, emdash?.config)}/_emdash`,
diff --git a/src/astro/routes/api/auth/oauth/[provider]/callback.ts b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
index e49c2acdc30c00c61449ceb4e59b3ef7514f3572..e8c6c95d092774d76a4563779049f268c2803d7a 100644
--- a/src/astro/routes/api/auth/oauth/[provider]/callback.ts
+++ b/src/astro/routes/api/auth/oauth/[provider]/callback.ts
@@ -22,6 +22,9 @@ import { finalizeSetup } from "#api/setup-complete.js";
 import { createOAuthStateStore } from "#auth/oauth-state-store.js";
 import { OptionsRepository } from "#db/repositories/options.js";
 
+// Astro v6 removed locals.runtime.env (it now throws). Use cloudflare:workers for env access.
+import { env as _cfEnv } from "cloudflare:workers";
+
 type ProviderName = "github" | "google";
 
 const VALID_PROVIDERS = new Set<string>(["github", "google"]);
@@ -116,11 +119,9 @@ export const GET: APIRoute = async ({ params, request, locals, session, redirect
 
 	try {
 		// Get OAuth providers from environment
-		// eslint-disable-next-line typescript/no-unsafe-type-assertion -- locals.runtime is injected by the Cloudflare adapter at runtime; not declared on App.Locals since the adapter is optional
-		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
-		// eslint-disable-next-line typescript/no-unsafe-type-assertion -- import.meta.env is typed as ImportMetaEnv but we need Record<string, unknown> for getOAuthConfig
-		const env = runtimeLocals.runtime?.env ?? (import.meta.env as Record<string, unknown>);
-		const providers = getOAuthConfig(env);
+		// Astro v6 + Cloudflare adapter: use cloudflare:workers env (static import above)
+		// eslint-disable-next-line typescript/no-unsafe-type-assertion
+		const providers = getOAuthConfig(_cfEnv as unknown as Record<string, unknown>);
 
 		if (!providers[provider]) {
 			return redirect(
diff --git a/src/astro/routes/api/auth/oauth/[provider].ts b/src/astro/routes/api/auth/oauth/[provider].ts
index a9706888fcac56cafca29ecf43fd5e5e0b04b38b..84a08c4e242d94044291f63c592f053aac8be464 100644
--- a/src/astro/routes/api/auth/oauth/[provider].ts
+++ b/src/astro/routes/api/auth/oauth/[provider].ts
@@ -13,6 +13,9 @@ import { createAuthorizationUrl, type OAuthConsumerConfig } from "@emdash-cms/au
 import { getPublicOrigin } from "#api/public-url.js";
 import { createOAuthStateStore } from "#auth/oauth-state-store.js";
 
+// Astro v6 removed locals.runtime.env (it now throws). Use cloudflare:workers for env access.
+import { env as _cfEnv } from "cloudflare:workers";
+
 type ProviderName = "github" | "google";
 
 const VALID_PROVIDERS = new Set<string>(["github", "google"]);
@@ -94,12 +97,9 @@ export const GET: APIRoute = async ({ params, request, locals, redirect }) => {
 		const url = new URL(request.url);
 
 		// Get OAuth providers from environment
-		// Access via locals.runtime for Cloudflare, or import.meta.env for Node
-		// eslint-disable-next-line typescript/no-unsafe-type-assertion -- locals.runtime is injected by the Cloudflare adapter at runtime; not declared on App.Locals since the adapter is optional
-		const runtimeLocals = locals as unknown as { runtime?: { env?: Record<string, unknown> } };
-		// eslint-disable-next-line typescript/no-unsafe-type-assertion -- import.meta.env is typed as ImportMetaEnv but we need Record<string, unknown> for getOAuthConfig
-		const env = runtimeLocals.runtime?.env ?? (import.meta.env as Record<string, unknown>);
-		const providers = getOAuthConfig(env);
+		// Astro v6 + Cloudflare adapter: use cloudflare:workers env (static import above)
+		// eslint-disable-next-line typescript/no-unsafe-type-assertion
+		const providers = getOAuthConfig(_cfEnv as unknown as Record<string, unknown>);
 
 		if (!providers[provider]) {
 			return redirect(
