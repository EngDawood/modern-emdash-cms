diff --git a/dist/context-C9PB8vGd.mjs b/dist/context-C9PB8vGd.mjs
index f61523476ede6c9de51332e5ac83631a6276df62..c63d340eb97427ed1d9ff4551ef497621c781ddd 100644
--- a/dist/context-C9PB8vGd.mjs
+++ b/dist/context-C9PB8vGd.mjs
@@ -831,9 +831,10 @@ function createContentAccessWithWrite(db, beforeContentWrite) {
 	return {
 		...createContentAccess(db),
 		async create(collection, data, options) {
-			const locale = resolveContentCreateLocale(options?.locale);
 			await beforeContentWrite?.();
 			const { fields, seo } = splitSeoFromInput(data);
+			const { slug, status, authorId, primaryBylineId, locale: dataLocale, translationOf, publishedAt, createdAt, ...customFields } = fields;
+			const locale = resolveContentCreateLocale(options?.locale ?? dataLocale);
 			let contentMutated = false;
 			try {
 				const created = await withTransaction(db, async (trx) => {
@@ -842,7 +843,14 @@ function createContentAccessWithWrite(db, beforeContentWrite) {
 					const hasSeo = await assertSeoEnabled(trxSeoRepo, collection, seo);
 					const item = await trxContentRepo.create({
 						type: collection,
-						data: fields,
+						slug,
+						status,
+						authorId,
+						primaryBylineId,
+						translationOf,
+						publishedAt,
+						createdAt,
+						data: customFields,
 						locale
 					});
 					contentMutated = true;
@@ -871,6 +879,7 @@ function createContentAccessWithWrite(db, beforeContentWrite) {
 		async update(collection, id, data) {
 			await beforeContentWrite?.();
 			const { fields, seo } = splitSeoFromInput(data);
+			const { slug, status, publishedAt, scheduledAt, authorId, primaryBylineId, ...customFields } = fields;
 			const hasFieldUpdates = Object.keys(fields).length > 0;
 			let contentMutated = false;
 			try {
@@ -878,7 +887,15 @@ function createContentAccessWithWrite(db, beforeContentWrite) {
 					const trxContentRepo = new ContentRepository(trx);
 					const trxSeoRepo = new SeoRepository(trx);
 					const hasSeo = await assertSeoEnabled(trxSeoRepo, collection, seo);
-					const item = hasFieldUpdates ? await trxContentRepo.updateDraftAware(collection, id, { data: fields }) : await (async () => {
+					const item = hasFieldUpdates ? await trxContentRepo.updateDraftAware(collection, id, {
+						slug,
+						status,
+						publishedAt,
+						scheduledAt,
+						authorId,
+						primaryBylineId,
+						data: customFields
+					}) : await (async () => {
 						const existing = await trxContentRepo.findById(collection, id);
 						if (!existing) throw new Error("Content not found");
 						return existing;
diff --git a/src/plugins/context.ts b/src/plugins/context.ts
index 281dea566e800043b1899c9e1dacb37d985a3883..13c6667728bd5713478fcfa966b40e3fe52feca4 100644
--- a/src/plugins/context.ts
+++ b/src/plugins/context.ts
@@ -401,9 +401,20 @@ export function createContentAccessWithWrite(
 			data: ContentWriteInput,
 			options?: ContentCreateOptions,
 		): Promise<ContentItem> {
-			const locale = resolveContentCreateLocale(options?.locale);
 			await beforeContentWrite?.();
 			const { fields, seo } = splitSeoFromInput(data);
+			const {
+				slug,
+				status,
+				authorId,
+				primaryBylineId,
+				locale: dataLocale,
+				translationOf,
+				publishedAt,
+				createdAt,
+				...customFields
+			} = fields;
+			const locale = resolveContentCreateLocale(options?.locale ?? dataLocale);
 			let contentMutated = false;
 
 			try {
@@ -415,7 +426,14 @@ export function createContentAccessWithWrite(
 
 					const item = await trxContentRepo.create({
 						type: collection,
-						data: fields,
+						slug: slug as string | null | undefined,
+						status: status as string | undefined,
+						authorId: authorId as string | undefined,
+						primaryBylineId: primaryBylineId as string | null | undefined,
+						translationOf: translationOf as string | undefined,
+						publishedAt: publishedAt as string | null | undefined,
+						createdAt: createdAt as string | null | undefined,
+						data: customFields,
 						locale,
 					});
 					contentMutated = true;
@@ -455,6 +473,15 @@ export function createContentAccessWithWrite(
 		async update(collection: string, id: string, data: ContentWriteInput): Promise<ContentItem> {
 			await beforeContentWrite?.();
 			const { fields, seo } = splitSeoFromInput(data);
+			const {
+				slug,
+				status,
+				publishedAt,
+				scheduledAt,
+				authorId,
+				primaryBylineId,
+				...customFields
+			} = fields;
 			const hasFieldUpdates = Object.keys(fields).length > 0;
 			let contentMutated = false;
 
@@ -471,7 +498,15 @@ export function createContentAccessWithWrite(
 					// only the SEO table. updateDraftAware delegates no-op writes to
 					// ContentRepository.update.
 					const item = hasFieldUpdates
-						? await trxContentRepo.updateDraftAware(collection, id, { data: fields })
+						? await trxContentRepo.updateDraftAware(collection, id, {
+								slug: slug as string | null | undefined,
+								status: status as string | undefined,
+								publishedAt: publishedAt as string | null | undefined,
+								scheduledAt: scheduledAt as string | null | undefined,
+								authorId: authorId as string | null | undefined,
+								primaryBylineId: primaryBylineId as string | null | undefined,
+								data: customFields,
+							})
 						: await (async () => {
 								const existing = await trxContentRepo.findById(collection, id);
 								if (!existing) throw new Error("Content not found");
